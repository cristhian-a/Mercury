package com.next.graphics;

import java.awt.*;

/**
 * TO DO
 * Unificar nessa classe a renderização de objetos, e principalmente centralizar o scaling, para que
 * seja fácil trocar o up scaling entre a carga dos sprites ou durante a renderização.
 */
public class Renderer {

    public void drawImage(Graphics2D g2) {
    }
}
